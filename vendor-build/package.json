{
  "name": "plugmem-inspector-vendor-build",
  "private": true,
  "type": "module",
  "description": "One-time build of xyflow + React into a vendored bundle for the Memory Inspector. Run `npm install && npm run build` after bumping deps.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@dagrejs/dagre": "^1.1.4",
    "@xyflow/react": "^12.3.6",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
