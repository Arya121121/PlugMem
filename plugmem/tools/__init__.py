"""Developer tooling for the PlugMem codebase.

Tools in this package are not used at runtime — they exist to keep the
codebase in sync with itself (spec ↔ code) and to make routine
maintenance tasks scriptable.
"""
